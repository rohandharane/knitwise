{
  "name": "knitwise-web",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "KnitWise — Interactive knitting pattern reader",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "npx vercel --prod ..",
    "test": "vitest run",
    "upload-landing-media": "node scripts/upload-landing-demos-to-blob.mjs",
    "vercel-build": "node scripts/vercel-build.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.1"
  },
  "devDependencies": {
    "jsdom": "^29.0.1",
    "vitest": "^4.1.2"
  }
}

{
  "name": "knitwise-web",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "KnitWise — Interactive knitting pattern reader",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}

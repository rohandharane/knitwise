# Copy to .env.local for local `vercel dev` (never commit .env.local)
ANTHROPIC_API_KEY=

# Vercel Blob (optional: run `vercel env pull .env.local` from web/, or paste from dashboard)
# BLOB_READ_WRITE_TOKEN=
